# frifoto.emilbratt.no

Add images and or content to website by following three simple steps.

1. Put new images in ./img (if you want to add images).

2. Add image data and or edit other content in "admin.toml".

3. Run "main.py" to index all new images and render it into a JavaScript file "datamodel.js".
